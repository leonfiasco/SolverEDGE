export const metadata = {
  title: "About Us | SolverEDGE",
  description: "Learn more about our company",
};

import { AboutPage } from "../components"; // This will be the new client component

export default function About() {
  return <AboutPage />;
}
